// Test file for AI completions
function greet(name) {
  return "Hello, " + name;
}

// Type console.log(greet("
console.log(greet("World"));

// Test object completion
const user = {
  name: "Alice",
  age: 30
};

// Type user.
console.log(user.name);
